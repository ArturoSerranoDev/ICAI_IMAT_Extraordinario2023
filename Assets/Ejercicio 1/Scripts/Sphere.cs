using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sphere : MonoBehaviour
{
    // Como crear una esfera nueva
    //[SerializeField] private Sphere spherePrefab;
    //Sphere newSphere = Instantiate(spherePrefab, new Vector3(0, 0, 0), Quaternion.identity);

    // Como mover un objeto
    //newSphere.transform.position = new Vector3(0, 1, 0);

    // Metodo para saber si un usuario ha presionado uno de los numeros
    // de la parte superior del teclado
    //if (Input.GetKeyDown(KeyCode.Alpha1))
    //{

    //}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
